 ```bash 
dd if=/dev/sda of=disk.img
```

 ```bash 
dd if=/dev/zero of=test.img bs=1M count=100
```

 ```bash 
dd if=disk.img of=/dev/sdb

```

 ```bash 
dd if=ubuntu.iso of=/dev/sdX bs=4M status=progress
```

 ```bash 
dd if=/dev/zero of=file.img bs=1G count=1
```

 ```bash 
dd if=/dev/random of=random.bin bs=1M count=10
```

 ```bash 
dd if=ubuntu.iso of=/dev/sda
```

 ```bash 
lsblk
```

convert and copy file

data destroyer =dd

dd does what you say not what you mean

 ```bash 
dd if=/dev/zero of=/dev/sdc
```








